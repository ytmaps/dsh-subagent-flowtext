# Install-time bundle: ready immediately; the first invocation securely pairs with FlowText.
- insert:
    - id: flowtext-direct
      name: dsh-subagent-flowtext
      config:
        autoPair: true
        clientName: DeepSeek Harness
